{
  "name": "google-calendar-ical-server",
  "version": "1.0.0",
  "description": "A google calendar proxy service that exposes your calendar in iCal format for use in applications like MS Outlook.",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "ts-node": "^10.9.1",
    "typescript": "^4.7.4"
  },
  "dependencies": {
    "dotenv": "^16.0.1",
    "express": "^4.18.1"
  }
}
